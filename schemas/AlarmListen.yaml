allOf:
  - type: object
    description: Information about alarm listener
    nullable: true
    properties:
      statusCode:
        $ref: '../schemas/StatusCode.yaml'
  - $ref: '../schemas/Alarm.yaml'