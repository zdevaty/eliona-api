allOf:
  - type: object
    description: Information about asset data listener
    nullable: true
    properties:
      statusCode:
        $ref: '../schemas/StatusCode.yaml'
  - $ref: '../schemas/Data.yaml'