allOf:
  - $ref: 'AgentDeviceMappingGeneral.yaml'
  - type: object
    deprecated: true
    title: MbusAgentDeviceMapping
    description: Specific mapping for MBUS agents
    nullable: true
    properties:
      field:
        type: integer
        nullable: true
      scale:
        type: number
        format: double
        nullable: true
      zero:
        type: number
        format: double
        nullable: true
