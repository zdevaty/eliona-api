type: string
description: Type of asset heap data
default: input
enum:
  - input
  - info
  - status
  - output
x-enum-descriptions:
  - Input subtype for heap values
  - Info subtype for heap values
  - Status subtype for heap values
  - Output subtype for heap values
x-enum-varnames:
  - SUBTYPE_INPUT
  - SUBTYPE_INFO
  - SUBTYPE_STATUS
  - SUBTYPE_OUTPUT