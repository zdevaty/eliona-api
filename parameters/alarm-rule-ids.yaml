name: alarm-rule-ids
in: path
description: The ids of alarm rules
example: 4711
required: true
schema:
  type: array
  items:
    # Using string instead of integer, because go server code generation has a bug:
    # https://github.com/OpenAPITools/openapi-generator/issues/13029
    type: string