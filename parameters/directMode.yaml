name: directMode
description: Executes the operation directly without using other services.
in: query
required: false
example: true
default: false
schema:
  type: string
  enum:
    - true
    - false