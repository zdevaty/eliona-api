get:
  tags:
    - Data
  summary: Gets subtype data for an asset
  description: Gets information about subtype data for an asset.
  operationId: getDataByIdAndSubtype
  parameters:
    - $ref: '../parameters/asset-id.yaml'
    - $ref: '../parameters/data-subtype.yaml'
  responses:
    200:
      description: Successfully returned subtype data for an asset
      content:
        application/json:
          schema:
            $ref: '../schemas/Data.yaml'