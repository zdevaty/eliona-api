put:
  tags:
    - Dashboards
  summary: Create or update a widget type
  description: Create a widget type if the a type with the name not exists or update a widget type if the name already exists
  operationId: putWidgetType
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas/WidgetType.yaml'
  responses:
    201:
      description: Successfully added widget type